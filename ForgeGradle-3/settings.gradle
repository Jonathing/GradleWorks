import org.gradle.util.GradleVersion

// Enable the stable publishing preview feature
if (GradleVersion.current().getVersion().split('\\.').head().toInteger() < 5) {
    enableFeaturePreview('STABLE_PUBLISHING')
}
